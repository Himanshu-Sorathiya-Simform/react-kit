/**
 * Default debounce delay (ms) for `isScrolling` in `useVirtualList` and
 * `useVirtualGrid` - how long scrolling must stay idle before
 * `isScrolling` flips back to `false`.
 *
 * @defaultValue 150
 */
const SCROLLING_DEBOUNCE_MS = 150;

export { SCROLLING_DEBOUNCE_MS };
